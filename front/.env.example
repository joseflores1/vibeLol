# Frontend dev env. Copy to .env.development and fill in real values.
# This file is committed; .env.development is gitignored.

# Base URL of the vibeLol backend REST API. The frontend never calls Riot
# directly (see AGENTS.md §7).
VITE_API_URL=http://localhost:3000/api/v1